# Arena Stream 用户使用手册

## 快速开始

1. 首次使用双击 `setup_conda.bat` 安装环境。
2. 双击 `run.bat`，稍后浏览器会自动打开 Arena Stream。
3. 搜索球队、联赛、车手或赛事名称，或直接粘贴直播页面 URL。
4. 使用左侧分类切换足球、篮球、F1 和橄榄球。
5. 需要更广覆盖时保持“深度模式”开启；更在意速度时可关闭。
6. 点击结果卡片开始解析和播放。

## 播放方式

- YouTube 结果会使用官方嵌入播放器。
- `.m3u8` 地址由 hls.js 播放。
- `.flv` 地址由 mpegts.js 播放。
- `.mp4`、`.webm` 等浏览器支持的媒体由 HTML5 播放器播放。
- 其他公开页面依次尝试 Streamlink、yt-dlp、HTML/JSON 扫描和隔离浏览器网络观察。

播放器中的“打开原页面”可以回到内容来源；“复制媒体地址”仅在应用解析到直接媒体地址时可用。

## 常见问题

### 搜索很慢

深度搜索会并行查询多个入口，通常需要数秒到十几秒。网络、平台限流和直播状态都会影响速度；关闭“深度模式”可减少搜索量。

### 找到结果但无法播放

直播可能已结束、要求登录、限制嵌入、使用浏览器不支持的编码，或受到 DRM 保护。解析器检测到常见 DRM/许可证流量时会主动停止。此时请使用“打开原页面”在授权平台观看。

### 可以观看付费平台吗

本工具不会绕过订阅、登录、地区限制或 DRM。需要订阅的赛事应在官方页面或官方 App 中观看。

### 可以直接输入 m3u8 吗

可以。在搜索框粘贴完整 `https://...m3u8` 地址并搜索，然后点击生成的结果卡片。

### 为什么不再使用 PotPlayer

3.0 版把中文搜索、筛选、官方嵌入、HLS、HTTP-FLV 和 HTML5 播放整合到浏览器中，不要求额外安装桌面播放器。需要其他播放器时，可以复制成功解析后的媒体地址。

## 关闭程序

关闭启动脚本的命令行窗口，或在窗口内按 `Ctrl+C`。仅关闭浏览器标签不会停止本地服务。
