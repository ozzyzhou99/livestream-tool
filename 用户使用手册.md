# 直播解析工具 · 使用手册

> 把直播网页地址变成 PotPlayer 可以播放的流，画质更好、无广告、可截图录制。

---

## 一、准备工作

使用前请确认电脑上已安装以下软件：

| 软件 | 用途 | 下载地址 |
|------|------|----------|
| **PotPlayer** | 播放直播 | https://potplayer.daum.net |
| **直播解析工具** | 本工具（exe文件） | 由分享者提供 |

> 两个软件都安装好后，双击 `LiveStreamTool.exe` 即可启动，**无需安装 Python 或其他环境**。

---

## 二、界面说明

```
┌─────────────────────────────────────────────────────────────┐
│  直播地址: [_________________________________] 粘贴  清空  解析  │
│  防盗链 Referer: [______________________] 自动提取  清空        │
├─────────────────────────────────────────────────────────────┤
│  画质: [最佳画质 ▼]   ▶ 在 PotPlayer 中打开   📋 复制流地址    │
├─────────────────────────────────────────────────────────────┤
│  运行日志（显示操作结果）                                       │
├─────────────────────────────────────────────────────────────┤
│  PotPlayer 路径: [____________________] 浏览  自动检测  保存   │
├─────────────────────────────────────────────────────────────┤
│  历史记录（双击可重新打开）                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、使用方法

根据你的情况选择对应方式：

---

### 方式 A · 直接输入直播页面网址（推荐）

适用于 **Bilibili 直播、斗鱼、虎牙、Twitch、YouTube** 等主流平台。

**操作步骤：**

1. 复制直播页面的网址，例如：
   - `https://live.bilibili.com/123456`
   - `https://www.douyu.com/123456`
   - `https://www.twitch.tv/主播名`

2. 粘贴到「直播地址」框，点击 **「解析」**

3. 等待几秒，画质下拉框会自动出现可选项

4. 选择画质，点击 **「▶ 在 PotPlayer 中打开」**

> 如果解析成功，PotPlayer 会自动弹出并开始播放。

---

### 方式 B · 从浏览器手动获取流地址

适用于**小众体育直播网站、聚合直播站**等无法直接解析的网站。

#### 第一步：打开直播页面，等待视频开始播放

用 Chrome 浏览器打开直播网站，确认视频已经在播放中。

#### 第二步：打开开发者工具

按键盘 **F12**，屏幕右侧或底部会出现开发者面板。

#### 第三步：找到「网络」标签

点击顶部的 **「Network」（网络）** 标签。

#### 第四步：找到流地址

在 Network 面板里，点击 **「Fetch/XHR」** 类型按钮，然后**刷新页面（Ctrl+R）**。

等待视频重新开始播放，在请求列表里找一条包含 `room` 或 `live` 或 `stream` 字样的请求，点击它，在右侧 **「Preview」** 标签下查看内容。

找到类似下面这样的 JSON 数据：

```json
{
  "data": {
    "pull_flv_url": "https://play.xxx.com/live/stream.flv?txSecret=abc&txTime=123",
    "play_flow": [
      { "name": "高清", "login_status": 0, "play_url": "https://..." },
      { "name": "标清", "login_status": 0, "play_url": "https://..." }
    ]
  }
}
```

- 优先选择 `login_status: 0`（**不需要登录**）的地址
- 推荐使用「高清」或「标清」的 `play_url`
- **右键点击该 URL → 复制**

> **注意：这个地址有时效性（通常几小时内有效），每次看直播都需要重新获取。**

#### 第五步：粘贴到工具并填写 Referer

1. 将复制的流地址粘贴到「直播地址」框

2. 在「防盗链 Referer」框中**填写直播网站的域名**，例如：
   ```
   https://www.wqzb750.com/
   ```
   （或点击「**自动提取**」按钮，工具会从地址栏自动提取）

3. 点击 **「▶ 在 PotPlayer 中打开」**

---

### 方式 C · 直接粘贴流地址

如果你已经有 `.m3u8`、`.flv`、`rtmp://` 等格式的地址，可以直接粘贴进来，不需要点「解析」，直接点「在 PotPlayer 中打开」即可。

---

## 四、首次使用配置

### 设置 PotPlayer 路径

工具会**自动检测** PotPlayer 的安装位置。如果没有自动填入，请：

1. 点击「PotPlayer 路径」旁边的 **「浏览」** 按钮
2. 找到 PotPlayer 的安装目录，选择 `PotPlayerMini64.exe`（或 `PotPlayerMini.exe`）
3. 点击 **「保存」**，下次启动自动记住

---

## 五、常见问题

**Q：点「解析」后一直转圈，没有反应？**
> 解析需要联网请求，通常 5~15 秒。如果超过 30 秒仍无响应，可能该网站暂不支持自动解析，请改用「方式 B」手动获取地址。

**Q：提示「不支持该网站」？**
> 该平台没有对应的解析插件，请使用「方式 B」从浏览器手动获取流地址。

**Q：PotPlayer 打开了但黑屏/无画面？**
> 可能原因：
> - 流地址已过期，请重新从浏览器获取
> - 需要填写「防盗链 Referer」（填写直播来源网站的域名）
> - 直播已结束

**Q：提示「PotPlayer 路径不存在」？**
> 请重新设置 PotPlayer 路径（参考上方「首次使用配置」）。

**Q：在 Network 里找不到 .flv 或 .m3u8？**
> 部分网站的流地址藏在 XHR 接口的 JSON 响应里（而不是直接的媒体请求），按「方式 B 第四步」查找包含 `play_url` 字段的 JSON 响应。

**Q：每次都要重新获取地址，太麻烦了？**
> 是的，这类网站的流地址带有时效性防盗链参数，每次获取大约可使用几小时。历史记录里保存了之前的地址，但过期后需要重新获取。

---

## 六、支持的平台

| 类型 | 平台示例 | 是否需要手动获取地址 |
|------|---------|-------------------|
| 国内主流直播 | Bilibili、斗鱼、虎牙 | 不需要，直接粘贴页面网址 |
| 国际直播 | Twitch、YouTube 直播 | 不需要，直接粘贴页面网址 |
| 体育聚合站 | 各类体育直播网站 | 需要（方式 B） |
| 直链地址 | .m3u8 / .flv / rtmp | 不需要，直接粘贴地址 |

---

## 七、注意事项

- 本工具仅用于观看**公开免费**的直播内容，请勿用于付费或受版权保护的内容。
- 流地址含有时效参数，**请勿转发给他人**，对方可能无法使用。
- 工具不存储任何账号信息或个人数据，历史记录保存在本机 `~/.livestream_tool/config.json`。
